#include<iostream>
#include<math.h>
#include<string>
#include<algorithm>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    double p,a,b,c,d;
    cin>>p>>a>>b>>c>>d;
    cout<< p * ( sin(a*k+b)+cos(c*k+d)+2);
    return 0;
}